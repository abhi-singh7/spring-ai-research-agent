# Research Agent UI

Angular 18 frontend for the Research Agent application.

## Prerequisites
- Node.js 20+
- Angular CLI 18+

## Run
```bash
npm install
ng serve --proxy-config proxy.conf.json
```

The app will be available at http://localhost:4200, with API calls proxied to localhost:8080.
